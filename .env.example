# Copy this file to .env and fill the values
BOT_TOKEN=your_telegram_bot_token_here

# Comma-separated list of allowed user IDs (e.g. 12345,67890). Leave empty (or don't set) to allow everyone.
ALLOWED_USERS=

# Optional values
PORT=10000
TOKEN_FILE=token.json
CREDENTIALS_FILE=credentials.json
